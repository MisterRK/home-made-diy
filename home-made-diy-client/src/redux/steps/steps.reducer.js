import stepActionTypes from './steps.types'

const INITIAL_STATE = {
   steps: [],
}

const stepsReducer = (state = INITIAL_STATE, action) => {
   switch(action.type){

      default: return state
   }
}

export default stepsReducer